<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="mdTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mdGreen</item>
        <item name="colorPrimaryDark">@color/mdDarkGreen</item>
        <item name="colorAccent">@color/mdRed</item>
        <item name="android:windowBackground">@color/mdLightGreen</item>
    </style>

    <style name="SplashScreenStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/mdBlack</item>
    </style>

</resources>
